# Backend Configuration
PORT=8080
GIN_MODE=debug

# Database Configuration (REQUIRED)
# These must be set in production - no defaults provided for security
DB_USER=root
DB_PASSWORD=your_secure_password_here
DB_NAME=web_crawler_db

# Database Configuration (Optional - defaults provided)
DB_HOST=localhost
DB_PORT=3306

# JWT Configuration (REQUIRED)
JWT_SECRET=your_jwt_secret_key_minimum_32_characters_long

# Crawler Configuration (Optional - defaults provided)
CRAWLER_TIMEOUT=30
CRAWLER_USER_AGENT=WebCrawlerBot/1.0

# Frontend Configuration
VITE_API_URL=http://localhost:8080

# Development Notes:
# - Copy this file to .env and fill in your actual values
# - DB_USER, DB_PASSWORD, DB_NAME, and JWT_SECRET are required
# - Other variables have sensible defaults
